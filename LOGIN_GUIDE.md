# 登录功能使用指南

## 功能概述

现在应用已经集成了登录功能，支持两种用户类型：

### 1. 普通用户
- **用户名**: 输入任意数字（如：123, 456, 789等）
- **任务选择**: 
  - **Task A**: 基础商业计划书写作
  - **Task B**: 元反思工作台（当前平台）

### 2. 管理员
- **用户名**: 输入 "admin"（不区分大小写）
- **权限**: 进入后台管理界面，查看所有用户的聊天记录和统计数据

## 使用步骤

### 普通用户登录
1. 打开应用
2. 在用户名输入框中输入数字（如：123）
3. 选择任务类型（Task A 基础写作 或 Task B 元反思工作台）
4. 点击"开始任务"按钮

### 管理员登录
1. 打开应用
2. 在用户名输入框中输入 "admin"
3. 点击"管理员登录"按钮

## 界面功能

### Task A - 基础商业计划书写作
- **左侧写作工作区**：
  - 7个写作框架板块（用户痛点、市场分析、产品介绍等）
  - 每个板块有详细的写作指导和示例问题
  - 支持保存和清空功能
  - 全屏写作模式
- **右侧聊天区域**：
  - GPT写作助手，针对当前写作板块提供帮助
  - 示例问题快速提问
  - 聊天记录导出功能

### Task B - 元反思工作台（原有功能）
- Idea迭代画布
- Writing工作区
- GPT策略顾问聊天

### 管理员后台
- **用户统计**: 查看所有用户的使用情况、消息数量、活跃时间等
- **聊天记录**: 查看所有用户的聊天对话记录，支持按用户和任务类型筛选
- **数据管理**: 实时监控用户活动，分析使用模式

### 通用功能
- 头部显示用户信息和任务类型
- 普通用户显示：用户 [数字] + Task A/B
- 管理员显示：管理员
- 右上角有"退出登录"按钮
- **聊天记录自动保存**: 所有聊天对话都会自动保存到数据库

### 退出登录
- 点击右上角的"退出登录"按钮
- 系统会清空所有数据并返回登录页面

## 技术实现

### 文件结构
- `src/Login.js` - 登录组件
- `src/Login.css` - 登录页面样式
- `src/App.js` - 主应用（已集成登录状态管理）

### 状态管理
- `isLoggedIn`: 登录状态
- `userInfo`: 用户信息（包含用户名、角色、任务类型）

### 数据清理
退出登录时会清空所有应用数据，确保用户隐私安全。

## 注意事项

1. 用户名验证：
   - 普通用户必须输入纯数字
   - 管理员必须输入 "admin"（不区分大小写）

2. 任务选择：
   - 只有普通用户需要选择任务类型
   - 管理员登录时任务选择会被自动隐藏

3. 数据安全：
   - 退出登录会清空所有本地数据
   - 每次登录都是全新的工作环境

## 开发说明

如需修改登录逻辑或添加新功能，请参考以下文件：
- 登录组件：`src/Login.js`
- 样式文件：`src/Login.css`
- 主应用集成：`src/App.js`（第6-65行）
